{
  "version": 2,
  "builds": [
    { "src": "package.json", "use": "@vercel/static-build" }
  ],
  "outputDirectory": "dist/frontend/browser",
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
